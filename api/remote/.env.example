# PostgreSQL connection string (required)
DATABASE_URL=postgres://user:password@localhost:5432/kuro?sslmode=disable

# Optional server config
REMOTE_HTTP_ADDR=:8080
REMOTE_HTTP_SHUTDOWN_TIMEOUT=10s

# Optional logging config
REMOTE_LOG_LEVEL=info
REMOTE_LOG_DEV=false
